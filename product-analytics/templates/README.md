# Templates

This directory contains template configurations and patterns used by SDK implementations.

The templates are consumed by SDKs during their build process to maintain consistency across implementations and provide reference implementations or configuration templates.

## Contents

Each template is documented inline with usage instructions for SDK implementers.
